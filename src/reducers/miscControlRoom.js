const Fn = {
    WorldMap: 2500,
    MiniMap: 1500,
}

const miscControlRoomReducer = (state = Fn, action) => {
    switch(action.type){
        default:
            return {
                ...state,
                }
    }
}
export default miscControlRoomReducer;